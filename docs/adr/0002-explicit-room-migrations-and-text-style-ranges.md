---
status: accepted
---

# Room 演进使用显式迁移，日记样式按文本范围持久化

首轮真机反馈要求同一日记文本块内的任意选区可以拥有不同颜色和字号，而旧模型只有整行颜色。继续沿用整行字段会丢失用户选择，改成完整富文本引擎又会引入不必要的编辑器复杂度；因此保留有序 Journal Block，并在文本块内保存经过规范化的非重叠样式范围，仅支持有限颜色和字号。所有已安装数据库从 v1 起使用显式 Room Migration 和迁移回放测试，禁止 destructive fallback，习惯历史标题/颜色快照与日记样式在同一 v1→v2 迁移中补齐。
