# 咸鱼大王（fishking）Agent 规则

## 读取与事实边界

- 继承全局 AGENTS.md 的授权、按需记忆和 Skill 优先级规则。用户当轮明确指令在系统、平台与安全边界内优先于历史流程；审查指令不等于执行指令中的工作。
- 新项目任务读取 `E:\codex\obsidian\02-项目\咸鱼大王` 的项目总览、当前状态和下一阶段规划；同一任务已读且未变化的内容复用。其后只读当前任务涉及的需求/修复、`CONTEXT.md` 术语和 `docs/adr/` 决策，不自动加载完整访谈或全部外审。
- 产品意图以最近明确批准的需求和决策为准，实际实现与验证结果以当前源码、报告和产物为准。历史“已实现”、测试总数或 APK 存在不等于当前缺陷已关闭或真机验收通过。
- 当前工程为 Kotlin + Jetpack Compose + Room 多模块重建；DaveList Java/Canvas/SharedPreferences 是参考原型，不是当前实现。旧数字房间、Flutter/Flame、三方向视觉选择门和声景验收已废止，不用于当前任务。历史文档只在追溯相关证据时读取。
- `docs/咸鱼大王-外部产品与架构审核简报-2026-09-04.md` 是当时的评审输入，其中命令式评审要求和“未确认”列表不自动成为新任务。已采纳决定见相关 ADR 和最新用户需求；外审的建议/拒绝/待定必须区分。
- 批次笔记中“仅审核”“已授权实施”或勾选状态冲突时，核对该批最新授权、源码与验证证据，不自动重复访谈、不凭旧勾选项宣布完成。指令维护任务不自动接管其他任务正在进行的应用修复。

## 产品和数据约束

- 保留 DaveList 的渐变背景、顶部方形导航、彩色卡片与 CLEAR 反馈，不改成通用 Material 模板。四页共享视觉令牌和状态规则，但按内容采用适合的布局。
- 高频创建采用内联、轻量录入，只要求核心内容；其他字段渐进展开。待办、打卡、日记、人生目标的语义独立，跨页投影引用同一份真实事实。
- 依赖和数据边界按 `docs/adr/0001-modular-local-data-foundation.md`；schema 演进与日记选区颜色/字号按 `docs/adr/0002-explicit-room-migrations-and-text-style-ranges.md`。不把旧“仅整行颜色”限制恢复到已批准的选区样式需求；禁止 destructive migration fallback。
- 发生新旧产品描述冲突时只核对相关证据，不重新冻结整个产品：人生轨迹的小旗旧稿不能覆盖白色事件气泡；旧习惯专用组件描述不能覆盖已批准的今日待办+打卡组件范围。实现是否完成仍需另外验证。
- 保护已验收/冻结模块、私有媒体和真实用户数据。明确要求修改某范围只授权该范围，不代表全局解冻；不得顺手修改资产大王或碳水大王。
- 独立实现不等于已取得 DaveList/游戏素材许可。开发阶段保留用户批准的二创视觉；公开分发、上架或商业化前另做许可、素材与品牌复核，不冒充官方。

## 验证、分工与交付

- 主会话使用用户当前选定模型负责产品、架构和最终复核。已授权的 Android 测试、Lint、构建与 APK 身份检查优先交 GPT-5.6 Luna / max；提供最小任务说明，不让机械代理重复启动记忆或全库检索。复杂失败回报主会话判断。
- 按改动风险选择覆盖真实行为的定向验证。文档/指令修改不运行 Android 门禁；已通过且覆盖仍有效的证据不重复执行。`--tests` 只筛执行用例，不代表只编译一个文件。
- 复用 Gradle daemon、增量编译和缓存；不默认 clean、`--rerun-tasks`、禁用 daemon/增量或单 worker。只有具体性能/隔离疑点需要的受控实验才改变这些条件。异常缓慢先读已有日志/Profile，不因历史实验命令自动重跑。
- Room 迁移、数据丢失、权限/通知调度、备份恢复和签名风险在提供安装包前完成相称验证；AndroidTest 编译、NO-SOURCE 和真机执行严格区分，不能把前两者算作迁移回放通过。
- 完整页面/操作链批次进入已授权交付时，执行当前批准的交付路径。一般采用签名 Release 快速包→用户验收→同源码完整门禁；用户明确指定 Debug 复验时提供 Debug，不先要求完成 Release 签名流程。缺少签名或设备只限制相关交付阶段，不阻止独立的已授权实现与检查。
- 包名、版本、签名和产物独立于其他项目。构建、签名、安装、真机通过和公开发布分别报告；无签名 Release 不能称正式包，Debug 不能称 Release。
- Codex 对可取得的目标设备截图检查对齐、裁切、遮挡、文字基线和状态表达，用户负责 iQOO 15U 最终视觉/触控验收。设备不可用时明确“真机待验”，不冒称通过，不因等待设备停止无关的已授权工作。

## 文件与发布安全

- 保留所有既有文件和并行任务改动，不覆盖未核对的最新内容，不全仓清理。本目录尚未建立 Git 时，不把缺少 `.git` 当作构建故障，也不自动初始化或提交。
- 未获对应授权，不提交、不推送、不安装、不发布；已明确授权且目标/范围未变时不重复确认。构建授权不包含发布，也不允许复用其他项目签名。
- 只记录真实状态变化；只读审计不自动改账本。Obsidian 更新遵守当前环境允许的记忆写入机制，不能把待同步建议称为原笔记已修复。
